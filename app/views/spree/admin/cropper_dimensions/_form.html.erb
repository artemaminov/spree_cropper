<% content_for :page_actions do %>
  <%= button_link_to Spree.t(:back_to_resource_list, resource: plural_resource_name(Spree::CropperDimension)), spree.admin_cropper_dimensions_path, icon: 'arrow-left', class: 'btn-primary' %>
<% end %>

<%= form_for [:admin, resource] do |f| %>
  <div class="row" data-hook="admin_page_form_fields">
    <div class="col-lg-6" data-hook="admin_page_form_left">

      <%= field_container f.object, :name, class: ['form-group'] do %>
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', required: true %>
        <%= f.error_message_on :name %>
      <% end %>

      <%= field_container f.object, :preserve_ratio, class: ['form-group'] do %>
        <%= f.label :preserve_ratio %>
        <%= f.check_box :preserve_ratio, class: 'form-control' %>
        <%= f.error_message_on :preserve_ratio %>
      <% end %>

    </div>
    <div class="col-lg-6" data-hook="admin_page_form_right">

      <%= field_container f.object, :width, class: ['form-group'] do %>
        <%= f.label :width %>
        <%= f.text_field :width, class: 'form-control' %>
        <%= f.error_message_on :width %>
      <% end %>

      <%= field_container f.object, :height, class: ['form-group'] do %>
        <%= f.label :height %>
        <%= f.text_field :height, class: 'form-control' %>
        <%= f.error_message_on :height %>
      <% end %>

    </div>
  </div>

  <%= render "spree/admin/shared/#{resource.new_record? ? 'new' : 'edit'}_resource_links" %>
<% end %>
